{
  "actorSpecification": 1,
  "actorOutputSchemaVersion": 1,
  "title": "Notion YouTube Agent Output",
  "description": "Output produced by the Notion + YouTube AI Agent",
  "properties": {
    "task": {
      "title": "Task",
      "type": "string",
      "description": "The original task given to the agent",
      "template": "{{task}}"
    },
    "result": {
      "title": "Result",
      "type": "string",
      "description": "The final answer produced by the agent",
      "template": "{{result}}"
    },
    "steps": {
      "title": "Steps",
      "type": "string",
      "description": "Tool calls and intermediate steps taken by the agent (JSON array)",
      "template": "{{steps}}"
    },
    "model": {
      "title": "Model",
      "type": "string",
      "description": "The model name used",
      "template": "{{model}}"
    }
  }
}
