{
  "title": "Notion + YouTube AI Agent",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "task": {
      "title": "Task",
      "type": "string",
      "description": "Natural language task for the agent. Examples: 'List the 5 most recently edited pages in my Notion workspace' or 'Search YouTube for clerk creators and append the top 10 channels to my Marketing Research page'.",
      "editor": "textarea"
    },
    "llmBaseUrl": {
      "title": "LLM Base URL",
      "type": "string",
      "description": "OpenAI-compatible endpoint. Defaults to Apify's OpenRouter proxy, which bills LLM usage to your Apify credits.",
      "default": "https://openrouter.apify.actor/api/v1",
      "editor": "textfield"
    },
    "llmModel": {
      "title": "LLM Model",
      "type": "string",
      "description": "Model name in OpenRouter format (provider/model). Examples: anthropic/claude-sonnet-4-6, openai/gpt-4o, openrouter/auto.",
      "default": "anthropic/claude-sonnet-4-6",
      "editor": "textfield"
    },
    "llmApiKey": {
      "title": "LLM API Key",
      "type": "string",
      "description": "API key for a custom LLM endpoint. Only needed when llmBaseUrl is changed from the default Apify OpenRouter proxy. Falls back to LLM_API_KEY env var.",
      "isSecret": true,
      "editor": "textfield"
    },
    "authTimeoutSeconds": {
      "title": "Auth Timeout (seconds)",
      "type": "integer",
      "description": "How long the actor waits for the user to complete Notion authorization via the magic link before failing (default: 300 = 5 minutes).",
      "default": 300,
      "minimum": 30,
      "maximum": 900
    },
    "notionDefaultParentPageId": {
      "title": "Default Notion Parent Page ID",
      "type": "string",
      "description": "Optional parent page ID used to create a missing target Notion page. Defaults to NOTION_DEFAULT_PARENT_PAGE_ID when set.",
      "editor": "textfield"
    },
    "notionDefaultDatabaseId": {
      "title": "Default Notion Database ID",
      "type": "string",
      "description": "Optional database ID used to create a missing target Notion page. Defaults to NOTION_DEFAULT_DATABASE_ID when set.",
      "editor": "textfield"
    },
    "maxIterations": {
      "title": "Max Agent Iterations",
      "type": "integer",
      "description": "Safety limit on the number of LLM + tool call rounds.",
      "default": 10,
      "minimum": 1,
      "maximum": 50
    }
  },
  "required": ["task"]
}
